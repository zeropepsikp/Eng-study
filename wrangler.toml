# Cloudflare Worker + 정적 자산 설정
# name 은 대시보드의 Worker 서비스 이름과 정확히 일치해야 한다(다르면 새 Worker 생성).
name = "zeroengstudy"
main = "src/index.js"
compatibility_date = "2024-11-01"

[assets]
directory = "./public"
binding = "ASSETS"
